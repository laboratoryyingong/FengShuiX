name: fengshui_compass
description: ""
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_compass: ^0.8.1
  sensors_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  url_launcher: ^6.3.2

flutter:
  assets:
    - assets/fengshui/
    - assets/analysis/
  uses-material-design: true
